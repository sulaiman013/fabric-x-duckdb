/* ============================== VIEW 1: the pipeline ==============================
   How 50,000,001 rows get from a PostgreSQL instance on localhost to a Power BI
   report, and what each hop cost. Every figure here was measured on the running
   system and is recorded in BUILD_LOG.md; the UAT re-measures the live ones on
   every pass. Nothing on this page is an estimate except where it says modelled. */
VIEWS.pipeline={
 label:"Pipeline",
 eyebrow:"Architecture &middot; 50M rows, no import, about a second a query",
 h1:"How 50 million rows reach a report",
 context:"Open mirroring carries an on-premises PostgreSQL table into OneLake and keeps it live from the write-ahead log. DuckDB transforms it on a Fabric Python notebook, Spark writes V-Ordered Delta, and Direct Lake serves it with no copy. Click any component.",
 foot:"fabric-x-duckdb &middot; pipeline architecture &middot; figures measured on the running system, UAT 19 September 2026",
 plats:[
  {id:"platPg",x:48,y:226,w:648,h:560,ic:"postgres",lab:"<b>PostgreSQL 16</b> &middot; localhost &middot; on-premises",
   foot:'<b style="color:var(--ink)">1 table &middot; 100 columns, every one TEXT</b> &middot; loaded LOGGED, because logical replication never sees an UNLOGGED table'},
  {id:"platFab",x:722,y:226,w:1030,h:560,ic:"fabric",lab:"<b>Microsoft Fabric</b> &middot; trial capacity &middot; UK South",
   foot:"No import copy. The notebook writes Delta, the model reads those same files, and nothing is refreshed on a schedule",footX:360}],
 nodes:{
  raw:{x:232,y:300,w:236,h:150,ic:"postgres",t:"landing.raw_txn",s:"50,000,001 rows &middot; 48 GB",
   title:"landing.raw_txn",where:"PostgreSQL &middot; one wide landing table &middot; bigdata tablespace on D:",
   blurb:"A deliberately ungoverned extract: five date formats in one column, money as text, epoch integers beside ISO timestamps, 31 spellings of Malaysia, and 1.18% exact duplicate rows. Every column is TEXT on purpose, because a landing zone that rejects rows is not a landing zone.",
   kv:[["rows","50,000,001"],["columns",100],["size as loaded","48 GB"],["exact duplicate rows","593,209"],["load rate","76,465 rows/s"]],
   pts:["Loaded <b>LOGGED</b>. An UNLOGGED table is never captured by logical replication, so the mirror's CDC would silently see nothing.",
    "<code>_mirror_row_id</code> is added as a surrogate key: no subset of the 100 business columns is unique, so it is the only stable handle to a row, and it is what makes the 593,209 duplicates resolvable downstream."]},
  seed:{x:84,y:534,w:250,h:122,cls:"stage",g:"upload",t:"Seed",s:"snapshot_to_fabric.py",
   title:"The seed",where:"snapshot_to_fabric.py &middot; DuckDB reads PostgreSQL, azcopy uploads",
   blurb:"Establishes the baseline the change feed builds on. DuckDB reads the table out of PostgreSQL, writes zstd parquet, and azcopy pushes it into the mirror's landing zone.",
   kv:[["files",16],["size","10.5 GB zstd"],["runtime","29 min"],["compression","4.3x against 45 GB CSV"]],
   pts:["A change feed with no seed leaves Fabric holding only rows that changed after the slot was created, which is not a mirror.",
    "azcopy needs <code>--trusted-microsoft-suffixes</code> to talk to OneLake."]},
  cdc:{x:376,y:534,w:250,h:122,cls:"stage",g:"wal",t:"Change feed",s:"cdc_to_fabric.py",
   title:"The change feed",where:"cdc_to_fabric.py &middot; a logical replication slot, polled every 10 s",
   blurb:"Reads the write-ahead log through a <code>test_decoding</code> slot, turns each change into a parquet row carrying <code>__rowMarker__</code>, and drops it into the landing zone as the next 20-digit sequential file. Fabric's replication engine applies it.",
   kv:[["slot","fabric_cdc"],["wal_level","logical"],["synchronous_commit","on"],["poll","every 10 s"],["marker","0 insert, 1 update, 2 delete, 4 upsert"]],
   pts:["Files must be strictly sequential and the metadata written <b>last</b>: a <code>_metadata.json</code> that lands before its data produces a \"Parquet file size is 0 bytes\" latch.",
    "<b>Create the slot before the snapshot.</b> This project did it the other way round: two rows fell into the gap, one deleted and one inserted, and because the errors had opposite signs the row counts still matched exactly."]},
  mirror:{x:760,y:300,w:170,h:150,ic:"mirror",t:"Mirrored database",s:"raw_txn &middot; Delta",
   title:"sulaiman-postgres",where:"Fabric open mirroring &middot; landing zone to Delta",
   blurb:"Fabric's replication engine reads the landing zone and applies inserts, updates and deletes to a Delta table, keyed on <code>_mirror_row_id</code>. Read-only by design, and the only thing this pipeline treats as bronze.",
   kv:[["rows","50,000,002"],["key column","_mirror_row_id"],["status","Replicating"],["applied to Delta","Merge commits"]],
   pts:["Open mirroring is the right mechanism here because a PostgreSQL instance on <code>localhost</code> is not reachable by Fabric and is not a supported native source.",
    "The one extra row is the pre-slot deletion: a row removed at the source before the slot existed, which cannot be removed through the slot and needs a re-seed. The UAT measures it on every run rather than rounding it away."]},
  lake:{x:966,y:300,w:170,h:150,ic:"lakehouse",t:"fincrime",s:"Lakehouse &middot; shortcut",
   title:"fincrime",where:"Fabric Lakehouse &middot; a shortcut to the mirror, plus the gold layer",
   blurb:"A shortcut at <code>Tables/raw_txn</code> points at the mirrored table, so the notebook reads the mirror without copying it. The gold parquet, the Delta tables and every evidence file live here too.",
   kv:[["shortcut","Tables/raw_txn"],["gold Delta tables",9],["gold size","2.5 GB"],["evidence files",3]],
   pts:["The shortcut is what stops the transformation staging a copy before it starts: DuckDB reads the mirror in place with <code>delta_scan</code>.",
    "<code>build_gold.json</code>, <code>verify.json</code> and <code>readme_last_run.txt</code> are written by the pipeline itself and read back by the UAT."]},
  nb:{x:1172,y:290,w:200,h:160,ic:"notebook",t:"01_build_gold",s:"DuckDB &middot; 8 vCores",
   title:"01_build_gold",where:"Fabric Python notebook &middot; not Spark &middot; DuckDB 1.2.2",
   blurb:"The whole transformation on one node: clean, conform, score seven rules, deduplicate, build the star schema, write parquet. Generated from readable Python by build_gold_notebook.py, so the code validated on a laptop is byte for byte the code that runs here.",
   kv:[["kernel","Python 3.11, not Spark"],["vCores",8],["billed","4 CU"],["RAM seen","67.4 GB"],["DuckDB work, billed","770 s"],["session wall","895 s"]],
   pts:["The notebook records its own environment, including whether a Spark session exists. <code>spark_in_globals: false</code> is the evidence it ran on the Python kernel.",
    "<code>%%configure</code> must be the first cell, and building it with %-formatting turns <code>%%</code> into <code>%</code>, a line magic that does not exist.",
    "See the <b>Transformation</b> view for what happens inside it."]},
  spark:{x:1172,y:534,w:200,h:130,ic:"spark",t:"02_vorder_write",s:"V-Order &middot; 233 s",
   title:"02_vorder_write",where:"Fabric Spark &middot; the only Spark in the pipeline",
   blurb:"Rewrites the gold parquet as V-Ordered Delta. This is the one job DuckDB cannot do, and it is why Spark is still here.",
   kv:[["wall time","233 s"],["billed","8 CU, starter pool"],["tables",9],["files",36],["size","2.5 GB"]],
   pts:["DuckDB cannot write V-Order, writes Delta INSERT-only with no MERGE, and never writes checkpoints, so a table written entirely by DuckDB grows an unbounded transaction log.",
    "V-Order is what makes Direct Lake fast, so the split falls exactly on that boundary: DuckDB for the analytics, Spark for the write.",
    "Gold timestamps must be exported as instants. A naive column reads as <code>TIMESTAMP_NTZ</code> in Spark, which the Delta tables and Direct Lake do not accept, and it broke this write once."]},
  sm:{x:1418,y:300,w:150,h:150,ic:"semantic",t:"Direct Lake",s:"fincrime_model",
   title:"fincrime_model",where:"Direct Lake semantic model &middot; 9 tables, 8 relationships, 17 measures",
   blurb:"Reads the same Delta files the notebook wrote. No import, no scheduled refresh, only framing, which copies metadata rather than data.",
   kv:[["tables",9],["relationships",8],["measures",17],["first query after reframe","7 to 11 s"],["warm","about 1 s"]],
   pts:["The first query pays for transcoding the fact's columns into memory; every query after that answers in about a second over 49,406,792 rows.",
    "The UAT reconciles the model against the gold layer on every run: Transactions and Rule Firings must equal the verified Delta counts exactly."]},
  rpt:{x:1602,y:300,w:132,h:150,ic:"report",t:"FinCrime",s:"4 pages",
   title:"The report",where:"Power BI &middot; four pages, live on the Direct Lake model",
   blurb:"Not built from native visuals. Each page is a single HTML Content visual fed by a DAX measure that ships both a pre-aggregated cube and the JavaScript that renders it, with no CDN and no external dependency.",
   kv:[["pages",4],["cube shipped","2,508 cells"],["of the measure ceiling","about 4%"],["ceiling","about 2.1M characters"]],
   pts:["<code>build_report.py</code> generates the whole PBIP, including a native page navigator so the report is usable outside Desktop.",
    "Each page is generated and then bisected against Power BI Desktop through a bridge, because a measure this size is code and cannot be eyeballed."]}},
 brackets:[{x:84,y:700,w:542,tw:520,b:"SLOT BEFORE SNAPSHOT",
   s:"create the replication slot first, or a change in the gap is lost with no error and no count mismatch"}],
 chips:{
  seedpath:{amber:true,g:"zone",t:"seed",s:"16 parquet",title:"The seed path",where:"snapshot_to_fabric.py &rarr; LandingZone",
   blurb:"One-time baseline: 10.5 GB of zstd parquet uploaded into the mirror's landing zone before the change feed takes over.",
   kv:[["files",16],["size","10.5 GB"],["runtime","29 min"]],
   pts:["Runs once. Everything after it is the change feed."]},
  cdcpath:{g:"parquet",t:"changes",s:"__rowMarker__",title:"The change path",where:"cdc_to_fabric.py &rarr; LandingZone",
   blurb:"Every committed change becomes a parquet row with a <code>__rowMarker__</code> column, written as the next sequential file. The replication engine batches whatever has arrived and applies it as a Delta merge.",
   kv:[["cadence","every 10 s"],["naming","20-digit sequential"],["measured propagation","151 s to processedRows"]],
   pts:["An insert and a delete of one key inside a batch collapse to nothing; a delete then insert collapses to an upsert."]}},
 flow:{x:762,y:676,w:604,hd:"A committed change, from the write-ahead log to a Delta row",steps:[
  {k:"fwal",t:"WAL",s:"logical slot",svg:'<path d="M2.6 6.6h3.6l2 8.2 2.6-12.4 2.4 10.4 1.6-6h4.2"/>',
   title:"The write-ahead log",where:"PostgreSQL &middot; wal_level = logical",
   blurb:"Every committed change is already in the WAL. A logical replication slot decodes it into readable change records rather than raw page images.",
   kv:[["wal_level","logical"],["plugin","test_decoding"],["slot","fabric_cdc"]],
   pts:["The slot retains WAL until the consumer acknowledges it, so a stopped replicator grows the log rather than losing changes."]},
  {k:"fpq",t:"Parquet",s:"+ __rowMarker__",svg:'<rect x="2.6" y="3.4" width="7" height="7" rx="1.2"/><rect x="12" y="3.4" width="7" height="7" rx="1.2"/><rect x="2.6" y="11.6" width="7" height="7" rx="1.2"/><rect x="12" y="11.6" width="7" height="7" rx="1.2"/>',
   title:"Parquet with a row marker",where:"one file per batch, 20-digit sequential",
   blurb:"Each change becomes a row carrying the full record plus <code>__rowMarker__</code>, which tells the engine whether to insert, update, delete or upsert it.",
   kv:[["0","insert"],["1","update"],["2","delete"],["4","upsert"]],
   pts:["The key columns are declared once in <code>_metadata.json</code>, and that file must be written after the data it describes."]},
  {k:"fzone",t:"Landing zone",s:"OneLake",svg:'<path d="M2.6 7.4 11 3.2l8.4 4.2-8.4 4.2z"/><path d="M2.6 11.6 11 15.8l8.4-4.2"/>',
   title:"The landing zone",where:"the mirrored database's own OneLake folder",
   blurb:"The publisher's only contract with Fabric. Drop correctly named parquet in, and the replication engine picks it up; there is no API call and no pipeline to trigger.",
   kv:[["path","LandingZone/raw_txn"],["upload","azcopy"]],
   pts:["<code>--trusted-microsoft-suffixes</code> is required for azcopy to authenticate against OneLake."]},
  {k:"fapply",t:"Applied",s:"Delta merge",svg:'<path d="M4 3.6v5.4a4.6 4.6 0 0 0 4.6 4.6H11M16.8 3.6v5.4a4.6 4.6 0 0 1-4.6 4.6H11M11 13.6v4.8"/>',
   title:"Applied to Delta",where:"Fabric's replication engine",
   blurb:"The engine batches whatever has arrived and applies it as a Delta merge on the key column, then advances its own counter.",
   kv:[["commit type","Merge"],["counter","processedRows"],["measured","151 s from UPDATE to counter"]],
   pts:["<b>processedRows</b> is the cheap way to wait for a change to be consumed; the UAT polls it and then reads the row back by value."]}]},
 kpis:[
  {k:"krows",v:"49.4M",l:"rows served",s:"no import, no refresh",g:1,
   title:"49,406,792 transactions, held once",where:"gold fact_transaction &middot; Direct Lake",
   blurb:"50,000,001 source rows less 593,209 exact duplicates. The Delta files the notebook wrote are the same files the semantic model reads: nothing is imported, so there is no third copy to keep in sync.",
   kv:[["source rows","50,000,001"],["duplicates removed","593,209"],["gold rows","49,406,792"],["rule firings","65,175,479"],["alerts","1,423,088"]],
   pts:["A gold layer is materialised either way, into this lakehouse or into warehouse storage. What Direct Lake removes is the Import copy, not a warehouse one."]},
  {k:"kbuild",v:"12.8 min",l:"full rebuild",s:"DuckDB on 8 vCores",
   title:"The whole transformation in 12.8 minutes",where:"01_build_gold &middot; measured, run 4",
   blurb:"Count the source, build the star, write nine parquet files. One node, no shuffle, no cluster to start.",
   kv:[["count source","12.7 s"],["build star","729 s"],["write parquet","28 s"],["notebook work, billed","770 s"],["session wall","895 s"]],
   pts:["A notebook is billed while it executes, not for the time taken to start its session (Microsoft Learn, <i>Apache Spark billing and utilization</i>). The 770 s of work is what is billed; the gap to the 895 s wall is shown rather than hidden."]},
  {k:"kcost",v:"$0.29",l:"per full rebuild",s:"at most 1.37 CU-hours",g:1,
   title:"At most 1.374 CU-hours for a complete rebuild",where:"executing time, documented CU rates, UK South price",
   blurb:"The Python kernel at 8 vCores bills 4 CU, and only while it executes; the Spark starter pool bills a documented minimum of 8. Same work, half the rate, and no shuffle.",
   kv:[["DuckDB transform","0.856 CU-h"],["V-Order write","at most 0.518 CU-h"],["this pipeline","at most 1.374 CU-h"],["modelled on Spark","2.230 CU-h"],["price, UK South","$0.21 / CU-h"]],
   pts:["The transformation step costs <b>half</b>; the pipeline as a whole 38% less. The V-Order write records only a wall time, so its figure is an upper bound.",
    "The Spark alternative is modelled and deliberately tilted in its favour: it assumes Spark takes the same executing time, when a distributed engine on a job that fits one node is usually slower.",
    "Against a T-SQL Warehouse the rate is close, 0.538 CU per vCore against 0.5, but a Warehouse bills every vNode it allocates, busy or waiting. A production pilot, anonymised, measured about 60 CU-s per Warehouse engine-second against 4 in a notebook; at that rate this whole job would have to finish in 82 engine-seconds to match."]},
  {k:"klat",v:"~1 s",l:"warm query",s:"over the full fact",g:1,
   title:"About a second, over 49.4M rows",where:"measured in-capacity with sempy",
   blurb:"The first query after a reframe transcodes the fact's columns into memory and costs 7 to 11 seconds. Every query after that is about a second.",
   kv:[["headline KPIs","0.98 s warm"],["overview cube, 2,508 cells","1.22 s"],["rules by band","1.12 s"],["cold, first query","7 to 11 s"]],
   pts:["Nothing was copied into a warehouse to make this true. Direct Lake reads the V-Ordered Delta files in place."]},
  {k:"kuat",v:"36 / 36",l:"acceptance checks",s:"0 failed, 1 skipped",g:1,
   title:"36 passed, 0 failed, 1 skipped, of 37",where:"scripts/uat.py &middot; 19 September 2026",
   blurb:"A repeatable pass over the whole pipeline, not a checklist. It checks by value: a real UPDATE pushed through the WAL and chased into OneLake, the build compared rule by rule against a baseline, and a decision written and read back out of Delta.",
   kv:[["repository",5],["ingestion",10],["transformation",9],["semantic model",4],["report",5],["documentation",4]],
   pts:["The skip is the four-minute full scan of PostgreSQL, opted out of with a flag.",
    "It also checks the README: the layout must match the repository file for file, and the numbers it quotes must be the ones the pipeline produces."]}],
 edges:[
  {from:"raw",to:"seed",fs:"b",ts:"t",w:1.8,sp:3,k:40},
  {from:"raw",to:"cdc",fs:"b",ts:"t",w:1.8,sp:3,ph:.5,k:40},
  {from:"seed",to:"mirror",style:"amber",w:1.8,n:2,sp:6,chip:"seedpath",at:.5,dy2:34,bow:40},
  {from:"cdc",to:"mirror",w:2.2,n:3,sp:3,chip:"cdcpath",at:.5,dy2:10,bow:-20},
  {from:"mirror",to:"lake",w:2.2,n:2,sp:2.4},
  {from:"lake",to:"nb",w:2.4,n:3,sp:2.4,ph:.4},
  {from:"nb",to:"spark",fs:"b",ts:"t",w:2.2,n:2,sp:2.6,k:30},
  {from:"spark",to:"sm",w:2.2,n:2,sp:2.8,ts:"b",k:90},
  {from:"sm",to:"rpt",w:2.2,n:2,sp:2.2,ph:.5}]
};
